# LLM provider keys (do not commit real values)
#
# GEMINI_API_KEY is the active key used by the RAG pipeline:
#   - text-embedding-004 for chunk + query embeddings
#   - gemini-1.5-flash for synthesis
# Get one free at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# OPENAI_API_KEY is unused by the current pipeline. Kept here for reference
# in case the RAG provider is swapped back to OpenAI later.
OPENAI_API_KEY=

# SEC EDGAR requires a User-Agent header — replace with your name/email.
SEC_USER_AGENT="Rukhtam Amin rukhtam.amin@afiniti.com"
